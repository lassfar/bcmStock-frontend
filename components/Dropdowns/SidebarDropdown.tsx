import React from 'react';
import { ISidebarDropdownProps } from '@/components/types/props';


const SidebarDropdown: React.FC<ISidebarDropdownProps> = ({ text, icon, links }) => {
  return (
    <div className="">
      
    </div>
  );
};

export default SidebarDropdown;
